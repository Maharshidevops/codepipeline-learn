// Barrel for domain components (composite, feature-specific). Import as:
//   import { ResultCard, ProgressModal, LocationSelector } from '@/components/domain';

export { default as ProgressModal } from './ProgressModal/ProgressModal';
export { default as LocationSelector } from './LocationSelector/LocationSelector';
export type { LocationSelectorProps } from './LocationSelector/LocationSelector';
